# chatbot_web — Web Channel Pre-Login Agent
# Deployed to AWS BedrockAgentCore Runtime
# Same dependency pattern as chatbot/

# ── Bedrock AgentCore SDK ──────────────────────────────────────────────────
bedrock-agentcore>=1.14.1,<2.0.0

# ── Strands Agents — tool decorator + agent orchestration ─────────────────
strands-agents>=0.1.0

# ── AWS SDK ────────────────────────────────────────────────────────────────
boto3>=1.43.49,<2.0.0
botocore>=1.43.49,<2.0.0

# ── MCP client for AgentCore Gateway ──────────────────────────────────────
# Required for SigV4-signed MCP tool discovery and invocation
# (ships as part of strands-agents-tools or standalone mcp package)
httpx>=0.27.0,<1.0.0

# ── Web framework (local dev only — not used in AgentCore Runtime) ─────────
fastapi>=0.111.0,<1.0.0
uvicorn>=0.30.0,<1.0.0

# ── Data validation ────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# ── Config / utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0
requests>=2.32.0,<3.0.0
structlog>=24.1.0,<25.0.0

# ── Session store ─────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0,<3.0.0
